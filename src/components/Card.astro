---
const { title, description, link, category } = Astro.props;
---
<div class="bg-white rounded-lg shadow-md overflow-hidden myCard">
  <div class="p-4">
    <h2 class="text-xl font-bold mb-2">{title}</h2>
    <p class="text-gray-700 mb-4">{description}</p>
    <h6><strong>Categoría: {category}</strong></h6>
    <a href={link} class="text-blue-500 hover:underline">Leer más...</a>
  </div>
</div>

<style>
  .myCard:hover {
    box-shadow: 0px 0px 10px rgba(118, 8, 74, 0.708);
  }
</style>